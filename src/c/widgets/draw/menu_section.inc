// MenuSection widget draw
// MenuSection is a logical grouping widget - dividers are rendered by the menu itself
case COGITO_MENU_SECTION: {
  // No additional rendering needed here - dividers are handled by the menu
  break;
}
