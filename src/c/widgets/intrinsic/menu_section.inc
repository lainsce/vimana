// MenuSection widget intrinsic size
case COGITO_MENU_SECTION:
      // MenuSection is a logical grouping - doesn't take up visual space itself
      // It's used to add dividers between menu items
      w = 0;
      h = 0;
      break;
