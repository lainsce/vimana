case COGITO_SLIDER:
      w = 200;
      h = 48;
      if (n->kd->slider.size == COGITO_SLIDER_SIZE_M) h = 56;
      if (n->kd->slider.size == COGITO_SLIDER_SIZE_L) h = 72;
      if (n->kd->slider.size == COGITO_SLIDER_SIZE_XL) h = 112;
      break;
    
